{
  "name": "my-blog-static",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "deploy:pages": "./scripts/deploy-pages.sh",
    "deploy:pages:check": "./scripts/deploy-pages.sh --check",
    "deploy:pages:build-only": "./scripts/deploy-pages.sh --build-only",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.9",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "wrangler": "^4.40.0"
  }
}
