---
interface Props {
  title: string;
  color?: string;
}

const { title, color = "var(--accent)" } = Astro.props;
---

<div class="section-header">
  <div class="section-label" style={`background:${color}`}></div>
  <h2>{title}</h2>
</div>
