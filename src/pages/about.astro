---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getPublishedPosts } from "../lib/blog";

const posts = await getPublishedPosts();
const totalWords = posts.reduce((sum, post) => sum + (post.body ?? "").split(/\s+/u).filter(Boolean).length, 0);
const totalViews = posts.reduce((sum, post) => sum + post.data.views, 0);
---

<BaseLayout
  title="关于"
  description="关于 ZhuWei、博客写作方向与后续 Cloudflare + Astro 技术栈规划。"
  activePath="/about/"
  canonical="/about/"
>
  <main class="article-page-main">
    <div class="article-layout article-layout--single">
      <div>
        <article class="article-shell">
          <header class="article-head">
            <div class="article-meta">
              <span class="post-tag cyan">关于</span>
              <span class="article-meta-item">
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
                持续更新
              </span>
            </div>
            <h1 class="article-title">关于这个博客：Token 剩余产物</h1>
            <p class="article-summary">你可能在想，这个博客的更新频率是怎么确定的？是靠自律吗？是靠对技术的热爱吗？不，是靠我的 AI Token 余额。</p>
          </header>
          <div class="article-content">
            <h2>更新算法</h2>
            <p>很多人问我（其实并没人问）什么时候更新。答案很简单：<strong>每当我发现这个月的 AI Token 还没用完，又快到结算日的时候。</strong> 为了不浪费这些昂贵的字符，我会强行拉着 AI 跟我一起思考一些后端工程、系统架构或者“代码为什么跑不起来”的哲学问题。</p>
            <p>所以，如果你看到我高频更新，说明我最近比较闲或者 Token 买多了；如果我消失了，那说明我正在节衣缩食，或者正在手动打字（这太残忍了）。</p>
            
            <blockquote>“用代码书写，以文字沉淀”——其实真相是：“用 AI 吹水，以 Token 续命”。</blockquote>
            <p>欢迎常来，只要余额还在，我就还在。</p>

            <h2>关于作者</h2>
            <p><strong>ZhuWei</strong> <span class="post-tag cyan" style="margin-left:8px;">博主</span></p>
            <p>后端工程师 / 开源爱好者</p>
            <p>PHP · Go · Python，偏好分布式与 AI 工程化</p>
            <ul>
              <li>文章：{posts.length.toLocaleString("zh-CN")}</li>
              <li>字数：{totalWords.toLocaleString("zh-CN")}</li>
              <li>阅读：{totalViews.toLocaleString("zh-CN")}</li>
            </ul>
          </div>
        </article>
      </div>
    </div>
  </main>
</BaseLayout>
